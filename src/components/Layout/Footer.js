import { Card } from "react-bootstrap";
const Footer = props => {
    return (
        <Card>
            <Card.Footer>
                
            </Card.Footer>
        </Card>
    )
};

export default Footer;